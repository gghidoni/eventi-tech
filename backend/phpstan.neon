includes:
    - ./vendor/nunomaduro/larastan/extension.neon

parameters:
    paths:
        - app
    level: 5

    # Esclude cartelle che non ti interessano
    excludePaths:
        - tests/
        - database/
